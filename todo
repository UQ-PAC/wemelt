bare minimum necessary for AssignL rule
-make P_inv optional in parsing
-Gamma contains predicates
-Gamma stability check
--for all x in dom gamma,
  P && R ==> Gamma(x) == Gamma'(x)
-update P/Gamma - DONE
-include in R: x \in Locals => x == x' to fix issues with locals not handled properly

update P/Gamma - done
-update P with temp variables - done
-calculate P + R - done
--calculate dom m == new_var U weaker / equal - done
--do P + R update with R_var - done
-calculate {P, Gamma} + R - done
--calculate low_or_eq(P + R) - done
--if x in low_or_eq, add x -> t to gamma - done
--restrict gamma domain to low_or_eq - done
--apply mapping m to gamma - need to do when gamma update finished

for later
-fix r_var implementation
-if, while, do while rules
-quantification in predicates?